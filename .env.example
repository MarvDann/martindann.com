# Email Configuration (Server-Side with Nodemailer)
# Copy this file to .env and replace with your actual credentials

# SMTP Server Configuration
# For Gmail: smtp.gmail.com / Port: 587 (TLS) or 465 (SSL)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail Account Credentials
# IMPORTANT: For Gmail, you MUST use an "App Password", not your regular password
# Generate one at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Where to send contact form submissions
EMAIL_TO=your-email@gmail.com

# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Enable 2-Factor Authentication on your Gmail account
# 3. Generate an App Password at https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Other (Custom name)"
#    - Name it "Portfolio Website"
#    - Copy the 16-character password (no spaces)
# 4. Replace the values above with your actual credentials
# 5. Save the .env file (it will not be committed to git)
# 6. Restart your dev server for changes to take effect
#
# Note: These credentials are used server-side only and will NEVER be exposed in client JavaScript
# Security: App Passwords are safer than your main password and can be revoked anytime
